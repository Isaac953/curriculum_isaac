/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  basePath: "/cv_isaac",
  output: "export",
  reactStrictMode: true,
  distDir: 'dist',
  // images: {
  //   unoptimized: true,
  // },
}
 
module.exports = nextConfig
